# Sample Existing Conditions Preview

This folder is reserved for sample output files. The application generates `.docx` files locally under `outputs/Existing_Conditions_Output/`.

The repository keeps this lightweight markdown preview so GitHub has a stable sample-output folder without committing generated Word files.
