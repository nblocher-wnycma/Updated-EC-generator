# Section Prompt Templates

This folder is reserved for future section-specific prompt templates or JSON prompt definitions.

Version 1 stores the active section prompts in `src/section_templates.py` so the Tkinter app and command-line generator use the same source of truth.
